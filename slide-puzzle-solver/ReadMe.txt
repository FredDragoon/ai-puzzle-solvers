make sure to install the psutil package.

Pycharm/vscode should prompt when you highlight over it in the code in the main.py

this package/libary allows you to see things like CPU/Memory/Network/sensors in python in this 
particular program we use it to see memory usage.

go into main.py and run the main function and it should output the results of the various 
search algorithms